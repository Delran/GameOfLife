GUI :
    Edit mode:
        Paint scene
        Move scene with mouse
        Save with scenes
        Merge selected scenes into one

Game of Life:
    Show number of generations

Scene:

Code:
    Stop making use of matrix conversion function,
    refactorize every grid made with python list with numpy array

Bugs:

    Moving paint scenes cause them to not paint at the right spot
